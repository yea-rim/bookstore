<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lib">
   
   <select id="list" resultType="LibBookVO">
      select * from lib
   </select>
   
   <delete id="delete" parameterType="int">
      delete lib where lib_book_no = #{libBookNo}
   </delete>
   
   <insert id="insert" parameterType="LibBookVO">
      insert into lib(
            lib_book_no, lib_lib_info_no, lib_amount
         ) values (
            #{libBookNo}, #{libLibInfoNo}, #{libAmount}
         )
   </insert>
   
   <update id="update" parameterType="LibBookVO">
      update lib
         set
            lib_book_no = #{libBookNo},
            lib_lib_info_no = #{libLibInfoNo},
            lib_amount = #{libAmount},
         where
            lib_book_no = #{libBookNo}
   </update>
   
   <select id="one" resultType="LibBookVO" parameterType="int">
      select * lib where lib_book_no = #{libBookNo}
   </select>
   
   <select id="search" resultType="LibBookVO" parameterType="int">
      select * from lib
      where instr(lib_book_no, #{libBookNo}) > 0
      order by lib_book_no asc
   </select>

</mapper> 